#{ config, pkgs, inputs, lib,  ...}:{

  # programs = {
    # bash = {

    #   enable = true;
    #   shellAliases = {
    #     ls = "ls -lah --color";
    #     gs = "git status";
    #     nixb = "sudo nixos-rebuild switch --flake #johnny-home --show-trace";
    #   };
    #   sessionVariables = {
    #     EDITOR = "nvim";
    #   };
    #   initExtra = ''
    #   nv() { nvim -c "lcd $1" $1; }
    #   neofetch
    #   '';
    # };
#}
